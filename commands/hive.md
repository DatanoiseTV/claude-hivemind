---
description: Show who else is in this project's hive and how to collaborate with them
---

You are one instance in a Hivemind — multiple Claude Code instances collaborating on this project in real time.

1. Call the hive `whoami` tool to see your name and which peer instances are connected.
2. Call the hive `status` tool to see the shared task board, shared-context keys, locks, and roles.
3. Summarize for me: who is online and what they appear to be working on (use `changes` for recent file edits), and the state of the task board.

If there is obvious parallel work to divide, suggest how — but do not start until I say so.
