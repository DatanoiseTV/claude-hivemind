---
description: Send a message to all instances in this project's hive
argument-hint: <message>
---

Send this message to every peer instance in the hive using the `broadcast` tool:

$ARGUMENTS

Then confirm how many peers received it.
