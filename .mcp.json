{
  "mcpServers": {
    "hive": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/src/mcp-server.js"],
      "env": {
        "CLAUDE_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
